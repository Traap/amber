suite:
  name: ${file} 
  purpose: |

    This Test Suite demonstrates a future concept that  maybe implement.  The
    concepts include the items below.

    1) setup-before-all are Test Steps that are run before all Test Cases.
    2) setup-before-each are Test Steps that are run before each Test Case.
    3) teardown-after-all are Test Steps that are run before all Test Cases.
    4) teardown-after-each are Test Steps that are run before each Test Case.

  requirement: AMBER-IUR-nnn

# ------------------------------------------------------------------------------
setup-before-all:
  steps:
    - sudo: true
      command: which
      argument: echo 

    - sudo: false 
      command: which
      argument: ls 

# ------------------------------------------------------------------------------
setup-before-each:
  steps:
    - sudo: false 
      command: which
      argument: grep 

    - sudo: false 
      command: which
      argument: egrep 

# ------------------------------------------------------------------------------
teardown-after-all:
  steps:
    - sudo: false
      command: which
      argument: ssh

    - sudo: false
      command: which
      argument: curl 

# ------------------------------------------------------------------------------
teardown-after-each:
  steps:
    - sudo: false
      command: which
      argument: wget 

    - sudo: false
      command: which
      argument:  awk

# ------------------------------------------------------------------------------
includes:
  - case:
    - name: t001 
    - name: t002 
    - name: t003 
    - name: t004 
    - name: t005 
    - name: t006 

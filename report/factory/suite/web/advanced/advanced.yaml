suite:
  name: ${file}

  purpose: Validate Amber's reusable, application-neutral advanced web fixture.

includes:
  - folder: web/primitives
    case:
      - name: page-mock
      - name: choices
      - name: text-input
      - name: date-range
      - name: evidence

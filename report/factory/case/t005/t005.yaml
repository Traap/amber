case:
  name: ${file} 
  purpose: >
    This Test Case demonstrates embedded \LaTeX\ enumerate command.
  
    \begin{enumerate}
    \item Step #1 ls check
    \item Step #2 zip check 
    \end{enumerate}
  
  requirement: AMBER-IUR-006 

  steps:
    - confirm: Program ls has been installed.
      expectation: ls installation location is displayed.
      sudo: false 
      command: which
      argument: ls
      evidence: Starts on next line.

    - confirm: Program zip has been installed.
      expectation: zip installation location is displayed.
      sudo: false
      command: which
      argument: zip 
      evidence: Starts on next line.
